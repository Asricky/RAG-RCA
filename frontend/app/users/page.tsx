"use client";
import {FormEvent,useEffect,useState} from "react";import {ArrowPathIcon,UserPlusIcon,XMarkIcon} from "@heroicons/react/24/outline";import AppShell from "@/components/AppShell";import {api} from "@/lib/api";
type Row={id:string;name:string;email:string;role:"ADMIN"|"ANALYST";is_active:boolean};
export default function Users(){const[users,setUsers]=useState<Row[]>([]),[show,setShow]=useState(false),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState("");const[form,setForm]=useState({full_name:"",email:"",password:"",role:"ANALYST"});async function load(){setLoading(true);try{setUsers(await api<Row[]>("/users"))}catch(e){setError(e instanceof Error?e.message:"Gagal memuat users")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function create(e:FormEvent){e.preventDefault();setSaving(true);setError("");try{const user=await api<Row>("/users",{method:"POST",body:JSON.stringify(form)});setUsers(v=>[...v,user]);setShow(false);setForm({full_name:"",email:"",password:"",role:"ANALYST"});setMessage(`${user.name} berhasil dibuat.`)}catch(e){setError(e instanceof Error?e.message:"User gagal dibuat")}finally{setSaving(false)}}async function update(user:Row,payload:Partial<Row>){setSaving(true);setError("");try{const next=await api<Row>(`/users/${user.id}`,{method:"PATCH",body:JSON.stringify(payload)});setUsers(v=>v.map(x=>x.id===next.id?next:x));setMessage(`${next.name} berhasil diperbarui.`)}catch(e){setError(e instanceof Error?e.message:"User gagal diperbarui")}finally{setSaving(false)}}return <AppShell title="Users" subtitle="Manage workspace access and roles" actions={<><button className="icon-btn" onClick={load}><ArrowPathIcon/></button><button className="primary-small" onClick={()=>setShow(true)}><UserPlusIcon/>New user</button></>}><div className="page-content">{error&&<div className="error-banner"><span>{error}</span><button onClick={load}>Retry</button></div>}{message&&<div className="success-banner"><span>{message}</span><button onClick={()=>setMessage("")}>Dismiss</button></div>}<div className="panel"><table className="data-table"><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{loading?<tr><td colSpan={4} className="empty">Loading users…</td></tr>:users.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{x.email}</td><td><select className="table-select" value={x.role} disabled={saving} onChange={e=>update(x,{role:e.target.value as Row["role"]})}><option>ADMIN</option><option>ANALYST</option></select></td><td><select className="table-select" value={x.is_active?"ACTIVE":"DISABLED"} disabled={saving} onChange={e=>update(x,{is_active:e.target.value==="ACTIVE"})}><option>ACTIVE</option><option>DISABLED</option></select></td></tr>)}</tbody></table></div></div>{show&&<div className="modal-overlay" onClick={()=>setShow(false)}><form className="modal incident-form" onSubmit={create} onClick={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setShow(false)}><XMarkIcon/></button><h2>Create user</h2><label>Full name<input required minLength={2} value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Initial password<input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>ANALYST</option><option>ADMIN</option></select></label><div className="modal-actions"><button type="button" className="secondary-btn" onClick={()=>setShow(false)}>Cancel</button><button className="primary-small" disabled={saving}>{saving?"Creating…":"Create user"}</button></div></form></div>}</AppShell>}
